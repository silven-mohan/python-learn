# Exponentation Implementation:

base = int(input("Enter the base: "))       # Taking the base value
exp = int(input("Enter the exponent: "))        # Taking the exponent value.

print(f"{base} ^ {exp} = {base ** exp}")    # Prints the value.